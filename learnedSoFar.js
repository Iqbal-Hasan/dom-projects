/*
TODO variable --> var, let, const
    variable can store some information
    you can use that information later
    you can change that information later

? Rules for naming variable
    you cannot start with number
    you can use _(undersore) or $(doller) at start
    you cannot use spaces
    you camleCase
*/
// typeof -- number, string, boolean
// mathematical operator -- +(addition) -(subtraction) *(multiplication) /(division) %(remainder) **(exponent)
// mathematical operator shorthand -- +=, -=, *=, /=, %=, **=, ++, --
// parseInt, parseFloat, toFixed, toPrecision, toUpperCase(), toLowerCase()
// array -- arr.lenght, arr.indexof(), arr.includes(), arr.sort(), arr.push(), arr.pop(), arr.shift(), arr.unshift()
// &&[double and]-(all element need to true) ||[or]-(only one need to be true)
// if else
// while loop (variable, condition, loop bdoy, increase the number)
// for loop
// print a array with loop
// break and continue
// TODO function
// objects -- Object.key(Name), Object.values(Name)
// TODO for in and for of
// TODO Ojects.enterits
// switch
// TODO primitive and non-primitive
// TODO Arrow funciton
// spread operator

